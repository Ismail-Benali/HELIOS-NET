[package]
name = "helios_core"
version = "0.1.0"
edition = "2021"

[lib]
name = "helios_core"
crate-type = ["cdylib", "rlib"]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true

[dependencies]
